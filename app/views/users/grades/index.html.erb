<div class="container mt-4">
  <h1 class="h3 mb-4">Mis Calificaciones</h1>

  <ul class="nav nav-tabs mb-4" id="gradesTab" role="tablist">
    <li class="nav-item" role="presentation">
      <button class="nav-link active" id="semester1-tab" data-bs-toggle="tab" data-bs-target="#semester1" type="button" role="tab">
        Primer Semestre
      </button>
    </li>
    <li class="nav-item" role="presentation">
      <button class="nav-link" id="semester2-tab" data-bs-toggle="tab" data-bs-target="#semester2" type="button" role="tab">
        Segundo Semestre
      </button>
    </li>
    <li class="nav-item" role="presentation">
      <button class="nav-link" id="semester3-tab" data-bs-toggle="tab" data-bs-target="#semester3" type="button" role="tab">
        Tercer Semestre
      </button>
    </li>
  </ul>

  <div class="tab-content" id="gradesTabContent">
    <!-- Primer Semestre -->
    <div class="tab-pane fade show active" id="semester1" role="tabpanel">
      <% if @semesters[1].any? %>
        <div class="table-responsive">
          <table class="table table-bordered table-hover">
            <thead class="table-light">
              <tr>
                <th>Asignatura</th>
                <th>Nota</th>
                <th>Comentario</th>
                <th>Profesor</th>
                <th>Fecha</th>
              </tr>
            </thead>
            <tbody>
              <% @semesters[1].each do |grade| %>
                <tr>
                  <td><%= grade.subject %></td>
                  <td class="<%= grade.score < 6 ? 'text-danger' : 'text-success' %> fw-bold">
                    <%= grade.score %>
                  </td>
                  <td><%= grade.comment %></td>
                  <td><%= grade.admin.email %></td>
                  <td><%= grade.created_at.strftime("%d/%m/%Y") %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="alert alert-info">
          No hay calificaciones para el primer semestre.
        </div>
      <% end %>
    </div>


    <div class="tab-pane fade" id="semester2" role="tabpanel">
      <% if @semesters[2].any? %>
        <div class="table-responsive">
          <table class="table table-bordered table-hover">
            <thead class="table-light">
              <tr>
                <th>Asignatura</th>
                <th>Nota</th>
                <th>Comentario</th>
                <th>Profesor</th>
                <th>Fecha</th>
              </tr>
            </thead>
            <tbody>
              <% @semesters[2].each do |grade| %>
                <tr>
                  <td><%= grade.subject %></td>
                  <td class="<%= grade.score < 6 ? 'text-danger' : 'text-success' %> fw-bold">
                    <%= grade.score %>
                  </td>
                  <td><%= grade.comment %></td>
                  <td><%= grade.admin.email %></td>
                  <td><%= grade.created_at.strftime("%d/%m/%Y") %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="alert alert-info">
          No hay calificaciones para el segundo semestre.
        </div>
      <% end %>
    </div>


    <div class="tab-pane fade" id="semester3" role="tabpanel">
      <% if @semesters[3].any? %>
        <div class="table-responsive">
          <table class="table table-bordered table-hover">
            <thead class="table-light">
              <tr>
                <th>Asignatura</th>
                <th>Nota</th>
                <th>Comentario</th>
                <th>Profesor</th>
                <th>Fecha</th>
              </tr>
            </thead>
            <tbody>
              <% @semesters[3].each do |grade| %>
                <tr>
                  <td><%= grade.subject %></td>
                  <td class="<%= grade.score < 6 ? 'text-danger' : 'text-success' %> fw-bold">
                    <%= grade.score %>
                  </td>
                  <td><%= grade.comment %></td>
                  <td><%= grade.admin.email %></td>
                  <td><%= grade.created_at.strftime("%d/%m/%Y") %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="alert alert-info">
          No hay calificaciones para el tercer semestre.
        </div>
      <% end %>
    </div>
  </div>
</div>